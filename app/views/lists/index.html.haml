.col-md-6.col-md-offset-3
  %h3
    Hello! I`m Yours personal ToDo tasker
  %hr
  .lists
    - @lists.each do |item|
      %p.list-item
        = item.body
        %span
          = link_to "edit", edit_list_path(item), class:( 'btn btn-default  btn-xs')
          = link_to "delete", list_path(item), method: :delete, class:( 'btn btn-danger  btn-xs')
  %hr
  = link_to 'new task', new_list_path, class:( 'pull-right btn btn-info  btn-md')
